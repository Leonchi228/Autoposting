from sqlalchemy import Column, Integer, String, ForeignKey\nfrom sqlalchemy.orm import relationship\nfrom sqlalchemy.ext.declarative import declarative_base\n\nBase = declarative_base()\n\nclass Users(Base):\n    __tablename__ = 'users'\n    \n    id = Column(Integer, primary_key=True)\n    subscription_days = Column(Integer)\n    channel_ids = Column(Integer, ForeignKey('channels.id'))\n\n    channels = relationship("Channels", back_populates="owner")\n    \nclass Channels(Base):\n    __tablename__ = 'channels'\n    \n    id = Column(Integer, primary_key=True)\n    sources = Column(String)\n    owner_id = Column(Integer, ForeignKey('users.id'))\n    category = Column(String)\n\n    owner = relationship("Users", back_populates="channels")\n\n# Test Data Initialization\ndef init_test_data(session):\n    user = Users(id=1641734520, subscription_days=30)\n    session.add(user)\n    session.commit()\n